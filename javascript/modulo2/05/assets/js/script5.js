/* Retorno da função */

function nomeCompleto(nome, sobrenome) {
    return `${nome} ${sobrenome}`;
}
let completo = nomeCompleto("Rawston", "Marinho");
//ENTRADA -> PROCESSAMENTO -> SAÍDA

//console.log(nomeCompleto("Rawston", "Marinho"));